import ConthraxSB from '../assets/fonts/conthrax-sb.ttf';
import { createTheme } from '@mui/material/styles';
import { grey } from '@mui/material/colors';

export const mdTheme = createTheme({
  palette: {
    primary: {
      main: grey[900],
    },
  }
});

